services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: edda-postgres
    environment:
<<<<<<< Updated upstream
      POSTGRES_DB: edda
      POSTGRES_USER: edda
      POSTGRES_PASSWORD: edda
=======
      POSTGRES_DB: game_master
      POSTGRES_USER: game_master
      POSTGRES_PASSWORD: ${EDDA_POSTGRES_PASSWORD}
>>>>>>> Stashed changes
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U edda -d edda"]
      interval: 10s
      timeout: 5s
      retries: 5

  ollama:
    image: ollama/ollama:latest
    container_name: edda-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

volumes:
  postgres_data:
  ollama_data:
